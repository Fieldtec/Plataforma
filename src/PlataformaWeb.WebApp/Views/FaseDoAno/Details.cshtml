@model FaseDoAnoViewModel;
@inject PlataformaWeb.Business.Interfaces.IUser AppUser

@{ ViewData["Title"] = "Detalhe Fase: " + @Model.Nome; }

<div class="row align-items-center justify-content-center">    
    <div class="col-xl-7 col-lg-10">
        <fieldset class="form h-100" disabled>
            <legend class="FundoDestaque">Dados da Fase</legend>
            <div class="form-row">
                <div class="form-group col-lg-12">
                    <label asp-for="Nome" class="control-label"></label>
                    <input autofocus asp-for="Nome" class="form-control" />
                </div>
            </div>
            <div class="form-row">
                <div class="form-group col-md-6">
                    <label asp-for="DataInicio" class=" control-label"></label>
                    <input asp-for="DataInicio" asp-format="{0:dd/MM/yyyy}" type="text" class="form-control data" />
                </div>
                <div class="form-group col-md-6">
                    <label asp-for="DataFim" class=" control-label"></label>
                    <input asp-for="DataFim" asp-format="{0:dd/MM/yyyy}" type="text" class="form-control data" />
                </div>
            </div>
        </fieldset>
    </div>
</div>
<div class="text-center mt-3">
    @if (!AppUser.EhAdmin())
    {
<a autofocus asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning"><i class="fa fa-edit"></i>&nbsp;Editar</a>}
    <a asp-action="Index" class="btn btn-info"><i class="fa fa-times"></i>&nbsp;Voltar</a>
</div>

